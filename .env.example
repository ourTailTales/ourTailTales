# ourTailTales — copy to .env.local and fill in values
# cp .env.example .env.local

# App
NEXT_PUBLIC_APP_NAME=ourTailTales
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Server-only secrets (never prefix with NEXT_PUBLIC_)
# DATABASE_URL=
# AUTH_SECRET=
